name: Generate Dashboard

on:
  push:
    branches: [main]
    paths:
      - 'config.yaml'
      - 'src/**'
      - 'data/vulns.json'
      - 'data/npm-vulns.json'
      - '.github/workflows/generate.yml'
  schedule:
    # Run daily at 6:00 UTC
    - cron: '0 6 * * *'
  workflow_dispatch:

concurrency:
  group: generate
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync

      - name: Generate dashboard
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Build command with available vuln scan results
          CMD="uv run osp-dashboard collect"
          if [ -f data/vulns.json ]; then
            CMD="$CMD --vulns data/vulns.json"
          fi
          if [ -f data/npm-vulns.json ]; then
            CMD="$CMD --npm-vulns data/npm-vulns.json"
          fi
          $CMD

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: data/

  deploy:
    needs: generate
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
